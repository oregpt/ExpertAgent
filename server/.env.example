# =============================================================================
# Agent-in-a-Box v2 Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=4000
NODE_ENV=development

# -----------------------------------------------------------------------------
# Security (Phase 6)
# -----------------------------------------------------------------------------
# API key for admin routes. If not set, auth is disabled (dev mode).
API_KEY=your-admin-api-key-here

# Allowed CORS origins (comma-separated). Default: '*' (all origins) in dev.
CORS_ORIGINS=http://localhost:5173,https://your-domain.com

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Get a free database at: https://neon.tech or https://supabase.com
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# -----------------------------------------------------------------------------
# Default AI Model
# -----------------------------------------------------------------------------
# Options: claude-sonnet-4-20250514, claude-opus-4-20250514, claude-3-5-haiku-20241022
#          grok-3-latest, gemini-2.5-flash-preview-05-20
DEFAULT_MODEL=claude-sonnet-4-20250514

# -----------------------------------------------------------------------------
# LLM API Keys (add the ones you want to use)
# -----------------------------------------------------------------------------

# Anthropic (Claude) - https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (for embeddings) - https://platform.openai.com/
OPENAI_API_KEY=sk-proj-...

# xAI (Grok) - https://console.x.ai/
XAI_API_KEY=xai-...

# Google (Gemini) - https://aistudio.google.com/
GEMINI_API_KEY=AIza...

# -----------------------------------------------------------------------------
# Optional: Capability API Keys
# -----------------------------------------------------------------------------
# OPENWEATHER_API_KEY=your_key_here
# COINGECKO_API_KEY=your_key_here

# -----------------------------------------------------------------------------
# Feature Flags (Licensing)
# -----------------------------------------------------------------------------
# Enable/disable features for tiered pricing
# Set these for development or use a license key for production

# Multi-agent support (false = single agent only)
FEATURE_MULTI_AGENT=true
FEATURE_MAX_AGENTS=10

# Multimodal (file attachments in chat)
FEATURE_MULTIMODAL=true

# MCP Hub (capabilities system)
FEATURE_MCP_HUB=true

# Allowed capabilities (comma-separated, e.g., anyapi,calendar,weather)
FEATURE_CAPABILITIES=anyapi,calendar,weather

# Custom branding (false = shows "Powered by AgenticLedger")
FEATURE_CUSTOM_BRANDING=true

# -----------------------------------------------------------------------------
# v2 Feature Flags (new capabilities)
# -----------------------------------------------------------------------------

# Soul & Memory system (agent self-evolving personality + memory)
FEATURE_SOUL_MEMORY=true

# Proactive Engine (heartbeats, cron jobs, background tasks)
FEATURE_PROACTIVE=true

# Multi-Channel delivery (Slack, Teams, webhook — beyond widget)
FEATURE_MULTI_CHANNEL=true

# Deep Tools (web search, web fetch, memory search)
FEATURE_DEEP_TOOLS=true

# Background Agents (sub-agent spawning for parallel tasks)
FEATURE_BACKGROUND_AGENTS=true

# -----------------------------------------------------------------------------
# v2 Channel Configuration (if FEATURE_MULTI_CHANNEL=true)
# -----------------------------------------------------------------------------

# Slack Bot
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_SIGNING_SECRET=...
# SLACK_APP_TOKEN=xapp-...  (for Socket Mode)

# Microsoft Teams
# TEAMS_APP_ID=...
# TEAMS_APP_PASSWORD=...

# Web Search (for deep tools)
# BRAVE_API_KEY=...

# -----------------------------------------------------------------------------
# License Key (Optional - overrides feature flags above)
# -----------------------------------------------------------------------------
# In production, use a license key instead of individual FEATURE_* flags.
# The license key is a signed JWT containing all feature flags for a tier.
#
# Generate with:
#   LICENSE_SECRET=xxx npx ts-node scripts/generate-license.ts --org "Acme Corp" --tier pro
#   LICENSE_SECRET=xxx npx ts-node scripts/generate-license.ts --org "Acme Corp" --custom soulMemory,deepTools
#
# Tiers: starter (v1 equivalent), pro (+soul/memory/tools/channels), enterprise (everything)
#
# AGENTICLEDGER_LICENSE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Secret used for GENERATING license keys (only needed on the issuing machine)
# LICENSE_SECRET=your-super-secret-key-here
